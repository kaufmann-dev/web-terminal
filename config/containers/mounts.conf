# Intentionally empty.
#
# This overrides CentOS's distribution default, which bind-mounts RHEL
# subscription data into /run/secrets for every container. The development
# terminal does not consume host subscription data, and the automatic mount is
# incompatible with its deliberate single-UID rootless storage.
